onnx_path: "../ml_core/artifacts/change_model_v2/change_model_v2_int8.onnx"
metrics_path: "../ml_core/artifacts/change_model_v2/model_card.json"
threshold: 0.5
overlay_alpha: 0.4

# Experimental v3 (CBAM attention, boundary loss)
# Switch to this after validation confirms improvement over v2
# experimental_onnx_path: "ml_core/artifacts/change_model_v3/change_model_v3.onnx"
# experimental_val_iou: 0.7366
# Note: v3 uses 6-channel input [before+after concatenated], 
#       not two separate tensors — requires change_service.py update
