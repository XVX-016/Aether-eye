[pytest]
testpaths =
    tests
norecursedirs =
    .venv
    venv
    env
    dist
    build
markers =
    network: tests that require internet access
    torch: tests that require PyTorch and GPU/CPU runtime support
